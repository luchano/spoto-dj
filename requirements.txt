fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
librosa==0.10.2
numpy==1.26.4
soundfile==0.12.1
yt-dlp>=2025.1.1

# Local audio analysis pipeline (opt-in via USE_LOCAL_ANALYSIS=true).
# The downloader (zotify) is NOT listed here — it needs Python 3.10+ and lives
# in its own venv (.venv-dl), driven as a subprocess. See essentia_analysis.py.
essentia-tensorflow>=2.1b6.dev1110
